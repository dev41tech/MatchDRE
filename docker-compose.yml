services:
  backend:
    build:
      context: ./src/backend
    env_file:
      - ./src/backend/.env
    environment:
      PORT: 3011
    ports:
      - "3011:3011"
    restart: unless-stopped

  frontend:
    build:
      context: ./src/frontend
      args:
        VITE_API_URL: ""
    environment:
      API_UPSTREAM: http://backend:3011
    depends_on:
      - backend
    ports:
      - "85:80"
    restart: unless-stopped
